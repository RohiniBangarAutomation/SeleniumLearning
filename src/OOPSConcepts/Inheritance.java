package OOPSConcepts;

public class Inheritance {
	
	
	
	
	//by using inheritance the information becomes more managable
	//in other words we can say the inheritance is relationship between superclass and subclass
	//it will inherit the propertis from parent class to clild class
	//relationship is created by using keyword "extends"
	//child can inerit the protertis of parent but parent cannot inherit the child's properties
	//types of inheritance--
	//1.single
	//2.multiple
	//3.multilevel
	//4.hybrid
	//5.hierarchical
	
	//in java we use single mostly
	
	//class--class -extends
	//inheritance --inheritance-extends
	//class--interface--implements
	//interface-class(not possible)

}
